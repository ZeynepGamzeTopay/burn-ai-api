services:
  - type: web
    name: burn-ai-api
    env: python
    plan: standard
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn ai_server:app --bind 0.0.0.0:$PORT --timeout 120
    envVars:
      - key: GOOGLE_DRIVE_DOWNLOAD
        value: enabled